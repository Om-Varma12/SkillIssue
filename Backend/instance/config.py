# small config file for constants / future expansion
# currently not used heavily, but a good spot to put secrets / paths

ASSETS_FOLDER = None  # kept for future use; app.py computes assets path at runtime
MODEL_FOLDER = "models"
# small config file for constants / future expansion
# currently not used heavily, but a good spot to put secrets / paths

ASSETS_FOLDER = None  # kept for future use; app.py computes assets path at runtime
MODEL_FOLDER = "models"
